// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("simplate", "for simplate support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("simplate", "enable simplate support", "no");

if (PHP_simplate != "no") {
	EXTENSION("simplate", "simplate.cpp");
}

